<%= form_with(model: doctors_appointment) do |form| %>
  <% if doctors_appointment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(doctors_appointment.errors.count, "error") %> prohibited this doctors_appointment from being saved:</h2>

      <ul>
        <% doctors_appointment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
